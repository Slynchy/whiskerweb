function go(): void {
    // todo

    // load package.json
    // iterate over dependencies and peerdependencies
    // extract name and version and export to text file in the following style:
    // "pixi.js@8.0.0-rc @pixi-sound@4.2.0 etc. etc."

    // so that you can just do `npm install {cat textfile.txt}`
}

go();